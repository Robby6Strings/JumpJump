export enum Shape {
  Rectangle,
  Circle,
}

export enum Direction {
  Up,
  Down,
  Left,
  Right,
}

export enum GameObjectType {
  Unset,
  Player,
  Platform,
  Item,
}

export enum ItemType {
  Unset,
  Coin,
  Jetpack,
  Portal,
  Checkpoint,
}
