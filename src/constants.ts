const testMode: boolean = false

export const constants = {
  screenWidth: 640,
  screenHeight: 480,
  sectionHeight: 450,
  gravity: 1.25,
  friction: 0.5,
  shopDistance: testMode ? 5 : 50,
  testMode,
  testCoins: 51,
}
