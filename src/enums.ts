export enum Shape {
  Rectangle,
  Circle,
}

export enum Direction {
  Up,
  Down,
  Left,
  Right,
}

export enum GameObjectType {
  Unset,
  Player,
  Platform,
  Item,
  Turret,
  Projectile,
  Boss,
}

export enum ItemType {
  Unset,
  Coin,
  Portal,
  AntiGravity,
  Shop,
}

export enum StatusEffectType {
  Unset,
  Chill,
  Stun,
}
